package io.streamforge.transform.blueprint;

/** Allowlisted formatting operations that explicitly convert an exact field to a string. */
public enum BlueprintFormat {
  FIXED_DECIMAL_PLAIN,
  TIMESTAMP_ISO_UTC
}
