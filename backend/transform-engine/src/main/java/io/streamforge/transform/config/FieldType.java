package io.streamforge.transform.config;

/** Closed set of value types available to transformation rules. */
public enum FieldType {
  OBJECT,
  STRING,
  BOOLEAN,
  INT64,
  FIXED_DECIMAL,
  ENUM,
  TIMESTAMP_NANOS
}
